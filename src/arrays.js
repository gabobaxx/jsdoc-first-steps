// @ts-check

/**
 * Array of ages.
 * @type {Array}
 */

const age = ['21', 19, 20, 30, 80];

/**
 * Scores Array of just one data type.
 * @type {Array<Number>}
 */
const points = [10, 8, 7, 5];

/**
 * Scores Array of two data types
 * @type {Array<Number | String>}
 */
const scoresAndStrings = ['10', 20, '80'];

/**
 * Arrays of names.
 * @type {Array<String>}
 */
const names = ['Ryan', 'Fabian', 'Yosmer'];
names.push('Mariana');
