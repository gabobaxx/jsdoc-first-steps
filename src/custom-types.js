// @ts-check

/**
 * User
 * @typedef {Object} User
 * @property {number} id User Id
 * @property {string} name User's name
 * @property {number} [age] User's age (optional)
 * @property {boolean} active User state
 */

/**
 * @type {User}
 */
const myNewUser = {
	id: 1,
	name: 'Yose',
	age: 40,
	active: true,
};
/**
 * A new user
 * @type {User}
 */
const myNewUser2 = {
	id: 2,
	name: 'Jose',
	active: false,
};
